interface:
  display_name: "SCNet HPC"
  short_description: "Operate SCNet clusters with SSH, Slurm, and DCU profiles"
  default_prompt: "Use $scnet-hpc to inspect the selected cluster profile and prepare a validated Slurm workflow."

policy:
  allow_implicit_invocation: true
